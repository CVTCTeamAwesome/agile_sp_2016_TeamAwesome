<h1>Flash Cards</h1>
<div>
  <section>
    <h2>Deck</h2>
    <p>Select Your Deck</p>

  </section>  

</div>
<%= form_tag("/display", method: "get") do %>

  <%= select_tag 'deck_select', options_from_collection_for_select(@decks, "id", "title") %>
  <%= submit_tag("Search") %>
<% end %>


<span>Here is the name of the Deck with ID #2:</span>
<br>
<br>
<span style="color:blue;font-size:2em">
  
<% if defined?(@selectedDeck) && (@selectedDeck != nil) %>
  <% @bbb = Deck.find(@selectedDeck) %>
  <p>Here is the name of the Deck: <%= @bbb.title %></p>
  <p>Here is the id of the Deck: <%= @bbb.id %></p>
  <p>This deck has <%= @bbb.cards.size %> cards</p>
  <% @bbb.cards.each do |card| %>
  <%= card.id %>
  <% end %>
<% else %>
  <p>No Deck Selected</p>
<% end %>

  </span>