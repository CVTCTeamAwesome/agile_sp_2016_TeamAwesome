<h1>Card Editor</h1>

<div class="card_edit">
  
  <ul class="card_nav left">
    <li><%= link_to 'Show', deck_card_path(@deck, @card) %></li>
    <li><%= link_to 'Back', deck_cards_path(@deck, @cards) %></li>
  </ul>
  
  <%= render 'form' %>

</div>