<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @deck.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @deck.description %>
</p>

<p>
  <strong>Creation date:</strong>
  <%= @deck.creation_date %>
</p>

<p>
  <strong>Last modified:</strong>
  <%= @deck.last_modified %>
</p>

<%= link_to 'Edit', edit_deck_path(@deck) %> |
<%= link_to 'Back', decks_path %>

<%= link_to 'New Card', new_deck_card_path(:deck_id) %>
<%= link_to "manage cards", deck_cards_path(:deck_id) %>
