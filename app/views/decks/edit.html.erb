<h1>Editing Deck</h1>

<div class="deck_edit">
  
  <%= render 'form' %>

  <ul class="div_nav left">
    <li><%= link_to 'Show Deck', current_user_deck_path(current_user, @deck) %></li>
    <li><%= link_to 'Back to Decks', current_user_decks_path(current_user, @deck) %></li>
  </ul>

</div>